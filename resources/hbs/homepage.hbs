{{#> layout }}
  <h3 class="title">Welcome</h1>
  {{#with introduction}}
    {{> summary config=../config}}
  {{/with}}
  <h3 class="title">Most recent</h3>
  {{#each recents}}
    {{> summary config=../config}}
  {{/each}}
  <h3 class="title">Other recent by tag</h2>
  {{#each recentAmongTags}}
    <h1>"{{tag}}"</h1>
    {{#with entry}}
      {{> summary config=../config}}
    {{/with}}
  {{/each}}
  <h3 class="title">Featured</h2>
  {{#each featured}}
    {{> summary config=../config}}
  {{/each}}
{{/layout}}
